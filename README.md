# 🧩 Dynamic Events (Laravel Project)

A simple Laravel-based event management system with admin and public views.

## 🚀 Setup Instructions

```bash
# Clone the repository
git clone https://github.com/AmitDasadiya07/asstask.git
cd asstask

# Install dependencies
composer install

# Copy environment file
cp .env.example .env

# Run migrations with seeder
php artisan migrate --seed

# Run migrations
php artisan migrate

# Start the server
php artisan serve

